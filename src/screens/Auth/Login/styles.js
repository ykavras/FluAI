import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  inputs: {
    marginTop: 15,
    flexDirection: 'row',
    justifyContent: 'space-between',
  },
  input: {
    width: '49%',
  },
});
