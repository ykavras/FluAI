import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  customForm: {
    paddingBottom: 60,
  },
  formTab: {
    marginBottom: 16,
  },
  formBtnCustom: {
    marginTop: 10,
    marginBottom: 60,
  },
});
